# PeakRent.kz — Backend зависимости
# Установка: pip install -r requirements.txt

# ── Веб-фреймворк ─────────────────────────────────────────────────────────────
Flask==3.1.2

# ── База данных ───────────────────────────────────────────────────────────────
Flask-SQLAlchemy==3.1.1     # ORM для работы с PostgreSQL/SQLite
psycopg2-binary==2.9.9      # Драйвер PostgreSQL

# ── CORS ──────────────────────────────────────────────────────────────────────
Flask-CORS==4.0.0           # Разрешает запросы с фронтенда (localhost:3000)

# ── Авторизация ───────────────────────────────────────────────────────────────
PyJWT==2.7.0                # JSON Web Tokens

# ── AI / OpenAI ───────────────────────────────────────────────────────────────
openai==1.54.4              # GPT-4o-mini для рекомендаций

# ── HTTP запросы (погода, SMS) ────────────────────────────────────────────────
requests==2.32.5

# ── Переменные окружения ──────────────────────────────────────────────────────
python-dotenv==1.0.1        # Загрузка .env файла

# ── Продакшн сервер ───────────────────────────────────────────────────────────
gunicorn==23.0.0            # WSGI сервер для продакшна
